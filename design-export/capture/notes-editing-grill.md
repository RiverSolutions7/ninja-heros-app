# NotesDoc editing model — grill decisions
*visual-grill · 2026-07-12 · extends frames 8e/8e-edit (E1 inline + M3 menu stay locked)*

- critique door = HYBRID (River): "Critique" chip w/ AI mark on the developed card (beside "Review & edit ›") → opens NotesDoc with the critique input ready; the input itself LIVES in NotesDoc ✓
- critique input = A · MINI-DOCK (River): the capture pill, smaller, at the NotesDoc bottom — tap words to type, mic to speak (same waveform), ↑ applies ✓
- regen beat = A · IN-PLACE + GLOW + UNDO (River): "Revising…" in the mini-dock → changed rows resolve in w/ the develop-cascade language + a warm tint that fades ~2s → "Changed N steps · Undo" lozenge (whisper language, auto-fades); Undo restores the prior version; NO keep/discard gate ✓
- text door = B · FOOTER TEXT LINK (River): "✎ Edit as text", text-only, after the skills footer (doc keeps all content above the fold, so the bottom is near) ✓
- text mode = A · FULL-SCREEN CALM EDITOR (River): Cancel / Done top bar, one flowing text (steps as lines, "Cues:" paragraph), keyboard; Substack-lock shape ✓
- Done behavior = A · VERBATIM (River): line = step, "Cues:" paragraph = cues; instant, offline-safe, NO ✨ on Done — the AI never touches the text door (messy paste → run a Critique after) ✓
- add step = A · GHOST ROW (River): the next hairline numeral sits dimmed after the last step w/ "Add a step…" — tap → inline editor in place; doubles as the EMPTY-CARD state (zero steps → "1 Add a step…", never a dead end) ✓
- delete freedom = A · TOTAL FREEDOM + UNDO NET (River): Delete works on every step incl. the last; wipe-all = text-mode select-all; ANY destructive act surfaces the Undo lozenge ("Step deleted · Undo" / "Card cleared · Undo" — session-scoped, not forever-undo); emptied card = ghost row; cards save without steps. No confirms, no grayed buttons ✓
- states sweep (proposed defaults, River may veto): offline critique → 15d reassurance in the mini-dock, inline editing unaffected · critique API fail → 15c-style "Couldn't revise — try again" in the label slot, critique text preserved · AI returns no changes → "No changes made" lozenge, no glow · empty text on Done → steps=[] + cues=null + "Card cleared · Undo" · Cancel in text mode with edits → one small discard confirm (clean = closes silently) · mic denied on critique → typing fallback (15a pattern) · reduced motion → glow/cascade become plain fades · critique input capped ~1000 chars, API guards like /api/develop · Undo lozenge ~6s, replaced (never stacked) by newer actions, role=status + real button
- skills tap = RIDE ALONG (River): tap any skill pill (glimpse + doc footer) → a sheet of the selected curriculum's skills (TypeAgeSheet pattern: multi-select chips, AI picks pre-selected, "+ New skill" inline, Done commits); resolves dev_note f2f4cab7 ✓

## 🏁 GRILL COMPLETE — build prompts (fire in order; Opus, one worker at a time; attach this file + BUILD.md to each)

**N1 — The AI door: critique loop.** New `/api/revise` (clone the develop route's SDK pattern): POST {currentCard, critique, rawNote?} → revised DevelopResult; guards like /api/develop, critique capped 1000 chars. DevelopedCard gains a "✦ Critique" chip beside "Review & edit ›" (text-chip language, unauthored — flag) → opens NotesDoc with the mini-dock focused. NotesDoc gains the MINI-DOCK at its bottom: the capture pill scaled down (label "Tell it what to change…", tap words = typing door, mic = same waveform rig, ↑ = apply), solid lit navy, 44px disc min. Apply → "Revising…" (role=status) in the label slot → response lands IN PLACE: changed/added rows resolve with the develop-cascade language + a warm tint `rgba(255,90,31,0.10)` fading ~2s; the shared UNDO LOZENGE (W1 whisper geometry: soft glass rgba(12,19,34,0.78), r14, auto-fade ~6s, role=status + real button, newest replaces) shows "Changed N steps · Undo" — Undo restores the pre-revision card. States: offline → 15d reassurance in the mini-dock; API fail → "Couldn't revise — try again" (role=alert), critique text preserved; no-changes → "No changes made" lozenge, no glow; mic denied → typing (15a pattern); reduced motion → plain fades. Diffing: compare old/new steps by index+text to pick glow rows (changed, added; removed rows just leave).

**N2 — The coach door: Edit as text.** Footer text action "✎ Edit as text" (Inter 600 ~12.5px #9fb0c8, centered after the skills row, 44px hit-slop, no container) → FULL-SCREEN editor: top bar Cancel (left, #9fb0c8) / Done (right, white 600) — NO sparkle; body = one textarea, Inter 400 ~14.5px #e7eefa lh 1.7 on the screen base rgb(8,12,26), content = steps as plain lines + blank line + "Cues: <text>" when cues exist; keyboard-safe (visualViewport pin pattern from IdleDock). Done = VERBATIM: non-empty lines → steps (strip leading numbering if the coach typed numbers), a trailing "Cues:"-prefixed paragraph → cues (absent → cues unchanged? NO — absent → cues deleted IF it was present in the editor text when opened, i.e. the text is the whole truth); empty text → steps=[] + cues=null + "Card cleared · Undo". Cancel with edits → small discard confirm (screen-local pattern); clean → closes. Reduced motion: crossfade in/out; arrival = quick fade/slide consistent w/ the notes arrival.

**N3 — Step freedom.** Ghost add-row after the last step: the next hairline numeral (Inter 200 22px #5b6b86) + "Add a step…" at opacity ~0.45 → tap = new inline editor in place (commit adds; empty commit = no row). Delete enabled on EVERY step (remove chunk-4's last-step logic if any); every destructive act (step delete, cues delete — REMOVE chunk 12's unconfirmed-delete concern by adding undo here, wipe via N2) surfaces the shared Undo lozenge ("Step deleted · Undo" / "Cues deleted · Undo" / "Card cleared · Undo"). Emptied card = the ghost row IS the empty state ("1 Add a step…"). saveComponent already tolerates steps=[] (verify; description mapping = cues-only or null).

**N4 — Skills tap.** Skill pills on the glimpse + the doc footer become tappable (44px hit-slop, pill visuals untouched) → a sheet in the TypeAgeSheet pattern (same panel surface/radius/spring/scrim/Done): title "Skills", chips = the `skills` table rows filtered to the selected curriculums (age_group match; union across multi-select; dedupe by name), multi-select, the card's current skills pre-selected, "+ New skill" inline mini-form (name → insert {name, age_group: first selected curriculum} → selected), Done commits to flow state (and the save's skills array). Unauthored surface — stay inside the sheet's token language, flag values. Mark dev_note f2f4cab7 resolved on completion.

*(sequencing note: designed stack-agnostically — these re-express 1:1 in the Expo rebuild if the pivot lands; on web they're chunks 13–16 after the current fix stream.)*

references: Raycast Notes (AI actions ride the editor, no floating chrome) · WhatsApp Meta AI (speak+type in one dock) · Notion AI (explicit keep/discard after regenerate) · Substack composer (text mode = full calm editor).
reference lock: AI affordances ride existing input surfaces · regeneration never silently overwrites · text mode is a full editor, not a modal.
